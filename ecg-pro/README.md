# ECG Energy Monitoring System

See [PROJECT_README.md](./PROJECT_README.md) for PostgreSQL setup, device provisioning, the authenticated pulse API, and the custom Node / Socket.io server.

Run `npm run dev` for development and `npm run build` followed by `npm start` for production. The custom server is required for ingestion and realtime updates.